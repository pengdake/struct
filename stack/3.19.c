#include <stdio.h>
#include 'stack.h'
#define MAXLINE 30;
int compute(Stack S)
{
	char arr[MAXLINE];
	int i, ele_1, ele_2, result;
        char symbol[] = {'+', '-', '*', '/'};

	result = 0
        while((s = fgets(arr, MAXLINE, stdin)) != EOF)
	{
		len = 4
        	for(i = 0;i<len;i++ )
		{
			if(s == symbol[i])
			{
	            		ele_1 = Top(S);
				Pop(S);
				ele_2 = Top(S);
				pop(S);
                                result = ele_1
			}
			else
			{
				
			}
	}	
        
}
